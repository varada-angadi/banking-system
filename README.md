# banking-system
This backend banking system built with Node.js and Express handles user authentication, account management, and transactions. It uses MongoDB Atlas for data storage, JWT for secure login, bcrypt for password hashing, and Nodemailer for email notifications. APIs are tested via Postman.
